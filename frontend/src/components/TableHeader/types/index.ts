//  -------------- table header view list hook types ----------------------

export enum updateArgType {
  COLUMNS = 'columns',
  SORT = 'sort',
  FILTER = 'filter',
}

export enum updateViewArgType {
  PIN = 'pin',
  UNPIN = 'unpin',
}
