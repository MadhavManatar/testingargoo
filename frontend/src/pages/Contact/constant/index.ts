import { TAB } from 'components/EntityDetails/constant';

const TAB_LIST = [TAB.INFO, TAB.TIMELINE];

export const CONTACT_TABS = [...TAB_LIST];

export const DEAL_TABS = [...TAB_LIST];

export const LEAD_TABS = [...TAB_LIST];

export const ACTIVITY_TABS = [...TAB_LIST];
