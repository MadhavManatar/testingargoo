export interface SecurityAlertsTypes {
  location_alert?: boolean;
  invite_alert?: boolean;
  failed_login_alert?: boolean;
  reset_pass_alert?: boolean;
  del_pipeline_stage?: boolean;
  del_contact_alert?: boolean;
  del_account_alert?: boolean;
  del_deals_alert?: boolean;
  del_contact_count?: boolean;
  del_account_count?: boolean;
  del_deals_count?: boolean;
}
