// ** Components **
import Breadcrumbs from 'components/Breadcrumbs';
import { BREAD_CRUMB } from 'constant';

const AddStream = () => {
  return (
    <>
      <Breadcrumbs path={BREAD_CRUMB.addStream} />
      {/* <AddStreamModel /> */}
    </>
  );
};
export default AddStream;
